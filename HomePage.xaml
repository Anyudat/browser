<Page x:Class="browser.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="White">

    <Grid>
        <!-- Mini információs sáv -->
        <Border Width="38"
                Height="80"
                Background="#E6E6E6"
                CornerRadius="19"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="50,20,0,0"
                ToolTip="Contact Card and Information" 
               >

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Logo -->
                <Ellipse Width="30" Height="30" Fill="#B0B0B0"
                         Margin="0,6,0,12"
                         HorizontalAlignment="Center"/>

                <!-- Info button -->
                <Button x:Name="InfoButton"
                        Width="18"
                        Height="28"
                        HorizontalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="InfoButton_Click"
                        Margin="0,42,0,10"
                        Grid.RowSpan="2">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="HoverBorder" Background="Transparent" CornerRadius="22">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Ellipse Width="4" Height="4" Fill="#707070" Margin="0,3"/>
                                    <Ellipse Width="4" Height="4" Fill="#707070" Margin="0,3"/>
                                    <Ellipse Width="4" Height="4" Fill="#707070" Margin="0,3"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="HoverBorder" Property="Background" Value="#CFCFCF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Page>
